export enum EMessageContentType {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
  LOCATION = 'location',
}

export enum EMessageType {
  IN = 'in',
  OUT = 'out',
}

export enum EMessageStatus {
  READ = 'read',
  UNREAD = 'unread',
}
